
  # FoodWave (copia)

  This is a code bundle for FoodWave (copia). The original project is available at https://www.figma.com/design/jvKQw7uMAN1ZYEknLY1v2d/FoodWave--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  